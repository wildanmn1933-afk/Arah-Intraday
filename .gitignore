node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Basis data runtime: ditulis ulang oleh server saat ingest (timestamp, baris berita baru).
# Berisi hash kata sandi & data pengguna, jadi jangan di-commit. Seed membuat ulang skema + akun admin saat start.
data/*.db.json
